# Vite + React Example

`prompt-searcher` SDK 사용 예제입니다.

## 1) 설치

```bash
pnpm install
```

## 2) 환경 변수

```bash
cp .env.example .env
```

`.env`에 `VITE_DAIMS_API_KEY`를 설정하세요.

## 3) 실행

```bash
pnpm dev
```

브라우저에서 검색어로 `search`를 호출하고, 결과 항목에서 `Get Prompt`로 `getPrompt`를 호출할 수 있습니다.
